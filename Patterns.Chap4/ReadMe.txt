状态模式

状态模式属于行为分组

意图
State模式允许对象在其内部状态变化时改变自己的行为。这是通过切换实现状态依赖行为的内部状态对象来实现的。对象将所有基于状态的行为推给以来的状态子类，这减轻了在对象方法中对case语句的需求量。

示例
使用State模式来提供订单对象的状态行为。假设一张订单在任意时刻处于3种状态之一：New（新订单）、Shipped（已发货）、Canceled（已取消）。New状态的订单可以发货或者取消；Shippe和Canceled状态的订单不能被取消或发货。